

@{
    ViewBag.Title = "Cabinet";
}

<h2>My cabinet</h2>


<table style="margin:30px">


    <tr>
        <td>
            <h3>@ViewBag.User.UserName</h3>
        </td>

        <td>
           @Html.ActionLink("My lots", "MyLots", "Lot", null, new { @class = "btn btn-info btn-lg", @style="margin:10px" })

        </td>
     </tr>
 </table>

        @if (User.IsInRole("Admin"))
        {
            <div id="users">
                @Html.Partial("_UsersList")
            </div>
        }

